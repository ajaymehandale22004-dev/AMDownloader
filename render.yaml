services:
  - type: web
    name: amdownloader
    runtime: python

    buildCommand: |
      pip install -r requirements.txt
      curl -fsSL https://deno.land/install.sh | sh

    startCommand: |
      export PATH="$HOME/.deno/bin:$PATH"
      gunicorn app:app